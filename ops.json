{"v":1,"lamport":13,"ops":[{"kind":"append_entry","text":"EXTERNAL LANDING MID-P4 + P6 SCOPE ADJUSTMENT: another session shipped v13.2.0 (tagged + on PyPI) while the P4 fix cycle ran — cb0c560b feat(command) byte-span splice layer (Command.span, Occurrence, CommandLine.splice; pure Python in command.py, one new root export \"Occurrence\"), merged at a03d957e, guides re-render de7748a1 = new origin/main. Consequences: (1) p4-api (29 commits, review-complete, frozen at 38a4cb0c) re-opened for one rebase onto de7748a1 — expected single conflict in __init__.py (Occurrence joins the v14 EXPORTS + TYPE_CHECKING block; static==runtime pin enforces), mergiraf bypass + byte-verify mandated; (2) P6 SCOPE CHANGE: the Python command path was slated for deletion at P6, but the splice layer is NEW public pure-Python API with no Rust counterpart — P6 must either port span/Occurrence/splice into the core command module (and reconcile the 3 documented command divergences) or keep the Python command path; decide at P6 planning; (3) P7 no-fold rule now covers v13.0/13.1/13.2 CHANGELOG sections; v14 breaking notes are written against v13.2.0. P4 review summary for the record: refuter FIX-FIRST 2/8 refuted (fence underscore blind spot → delta-allowlist rewrite; golden matrix 11→21 cases + stderr + non-empty mcp fixture golden), finder FIX-FIRST 9 findings (fixed: store transaction ownership w/ TransactionConflictError — reproduced data loss; stream prune-skip; WorkflowCall.args stub lie + 161-override audit + committed None-gate (the hand-written seed stub carried the same lie — machine-generation vindicated); gen_watch_golden re-pointed, byte-identical regen; view_field structural registry — bonus: vacuous ViewLike protocol root-caused; discovery PathBuf boundary (option 2, contained) — downstream loud-error boundaries accepted as representation-blocked divergence, note a6953b09; accepted-as-is: negative --poll → P5 clap validation, find_in(limit=-1) OverflowError pinned, private models._native name loss)."}]}