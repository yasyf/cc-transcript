{"v":1,"lamport":14,"ops":[{"kind":"append_entry","text":"P4 LANDED: main = 271ce83a, pushed (fast-forward from de7748a1), CI watch running. 29 commits: the _native rename, sync API (parse/stream/discover/resolve, Watcher.tick, sqlite3 store/ledger/decisions, corrections over the native engine with the rebindable open seam), the v14 __init__ surface (static==runtime pinned), parser decoder-tree + TranscriptParser deletion, machine-generated _native.pyi via pyo3-stub-gen (two-way drift gate; 161-override audit + committed None-gate), fences rewritten (delta-allowlist import fence, ~3ms vs 15ms budget), docs green on the v14 surface (griffe static path engineered; dynamic permanently broken by UnionType aliases — note 4e18bcca, playbook doc c2e8a3eb), .gitattributes generated-markings, the full review fix cycle (refuter 2/8 refuted, finder 9 findings: store TransactionConflictError ownership fix for reproduced data loss, stream prune-skip, stub truth fixes, watch-golden regen, view_field structural registry, discovery PathBuf), 21-case stderr-pinned CLI golden matrix + non-empty mcp fixture golden, and the post-v13.2 rebase (Occurrence integrated into the v14 surface). Landing verification: lane's fresh full battery on 271ce83a + orchestrator cargo re-run + corrected behavioral canary (13,453 transcripts discovered as Path objects; parse live). THREE-LEG LANE SAGA recorded: two usage-limit deaths + repeated stall/wake cycles; adoption protocol worked — zero work lost across deaths. USER DECISIONS (2026-07-16): (1) P6 PORTS the splice layer (span/Occurrence/splice) to Rust core — not keeping the Python command path; (2) land now; (3) re-enter plan mode for P5–P8 before dispatching more work. Next: revised plan for P5 (CLI, + --poll clap validation), P6 (deletion sweep + splice port + 3 command divergences), P7 (release vs v13.2.0, no-fold ×3), P8 (fleet ×4)."}]}