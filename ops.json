{"v":1,"lamport":22,"ops":[{"kind":"append_entry","text":"P6 SUB-LANE 1 LANDED: main = 340cfcab, pushed, CI watch running. Fix cycle (3 commits after review): eafd8e96 splice accepts any Mapping (mapping-protocol iteration, not dict-only) + Python index semantics (isize keys, negative tuple-indexing, IndexError 'tuple index out of range' — the PanicException killed; NoSpan/Overlap ValueErrors verbatim); 01f45d41 golden generator pinned to the Python reference by exec'ing command.py's PRE-FLIP source in an isolated namespace (a preserved alias would NOT work — the Python method bodies resolve Command/Redirect/Occurrence late, post-flip → native; regeneration byte-identical to committed golden; header note: sub-lane 2 deletes generator + bodies, golden freezes as contract); ef93cf4d stub-gate strip_receiver fix (receiver-position-only; finder counterexample pinned). Wart note 9a98bc8 item 14: the regex-dialect split (API = exact Python re, CLI = Rust dialect) is an explicit per-surface contract. Orchestrator landing verification: all six finder counterexamples produce the Python oracle through _native (MappingProxyType, -1/-2 resolution, 2/-3 IndexError, lookahead via re), 91 targeted tests green. main had moved again (f2211721: external docs-infra — great-docs 0.15.x/griffe 2.1 pins, CI fragments, guides; ZERO file overlap); rebased in temp worktree w/ mergiraf bypass, TREE_EQUIVALENT proven (rebased tip ≡ battery tree + exactly the external files), landed FF. P6 SUB-LANE 2 DISPATCHED: p6-sweep lane (opus) off 340cfcab — Part A literals inversion (generated/*.rs → hand-owned, build_rust_literals.py dies, _native.embedded_literals() feeds Python builders, test_literals_parity.py → no-redeclaration check, show.rs SIGNAL/NOISE JSONs unified), Part B deletion sweep module-by-module (Python bodies of all ported domains incl. command.py + gen_splice_golden.py; runtime deps click/orjson/tree-sitter/aiosqlite/anyio out; fences tightened; LOC target 4,000–4,500). DELIBERATE SHAPE DEVIATION from the plan table: the sweep runs as ONE sequential opus lane, not a parallel fan-out — the deletions share one import graph (__init__.py/tests/fences) and a fan-out would race; recorded here as the orchestrator's call."}]}